<%= polaris_select(
  name: :date_range,
  label: "Date range",
  required: true,
  select_options: { 
    include_blank: true,
  },
  options: {
    "Today" => "today",
    "Yesterday" => "yesterday",
    "Last 7 days" => "lastWeek",
  },
) %>
