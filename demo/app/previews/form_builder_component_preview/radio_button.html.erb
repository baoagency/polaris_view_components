<%= form_with(model: product, builder: Polaris::FormBuilder) do |form| %>
  <%= form.polaris_radio_button(:access,
    value: :allow,
    label: "Allow access",
  ) %>

  <%= form.polaris_radio_button(:access,
    value: :deny,
    label: "Deny access",
  ) %>
<% end %>
