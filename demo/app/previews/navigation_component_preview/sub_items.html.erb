<%= polaris_navigation do |navigation| %>
  <% navigation.section do |section| %>
    <% section.item(
      url: "#",
      label: "Home",
      icon: "HomeMajor"
    ) %>
    <% section.item(
      url: "#",
      label: "Orders",
      icon: "OrdersMajor"
    ) %>
    <% section.item(
      url: "#",
      label: "Products",
      icon: "ProductsMajor"
    ) do |item| %>
      <% item.sub_item(
        url: "#",
        label: "All products"
      ) %>
      <% item.sub_item(
        url: "#",
        label: "Inventory"
      ) %>
    <% end %>
  <% end %>
<% end %>
