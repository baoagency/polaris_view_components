<%= polaris_button do |button| %>
  <% button.with_icon(name: "PlusCircleIcon") %>
  Add product
<% end %>

<%= polaris_button(plain: true) do |button| %>
  <% button.with_icon(name: "PlusCircleIcon") %>
  Add product
<% end %>
