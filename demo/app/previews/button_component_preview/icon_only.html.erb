<%= polaris_button(url: "https://shopify.com") do |button| %>
  <% button.with_icon(name: "ArrowLeftMinor") %>
<% end %>

<%= polaris_button_to(Product.new) do |button| %>
  <% button.with_icon(name: "ArrowRightMinor") %>
<% end %>
