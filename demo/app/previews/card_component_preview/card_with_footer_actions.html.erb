<%= polaris_card(title: "Shipment 1234") do |card| %>
  <p>View a summary of your online store’s performance.</p>

  <% card.primary_footer_action(url: "#") { "Add tracking number" } %>
  <% card.secondary_footer_action(url: "#") { "Edit shipment" } %>
<% end %>
