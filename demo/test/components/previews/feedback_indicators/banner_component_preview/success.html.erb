<%= polaris_banner(
  title: "Your shipping label is ready to print.",
  status: :success,
) do |banner| %>
  <% banner.dismiss_button(url: "#") %>
  <% banner.action(url: '#') { "Print label" } %>
<% end %>
