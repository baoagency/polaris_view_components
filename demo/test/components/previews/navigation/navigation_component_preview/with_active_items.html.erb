<%= polaris_navigation do |navigation| %>
  <% navigation.section do |section| %>
    <% section.item(
      url: "#",
      label: "Home",
      icon: "HomeMajor",
      selected: true
    ) %>
    <% section.item(
      url: "#",
      label: "Orders",
      icon: "OrdersMajor"
    ) %>
    <% section.item(
      url: "#",
      label: "Products",
      icon: "ProductsMajor"
    ) do |item| %>
      <% item.sub_item(
        url: "#",
        label: "All products",
        selected: true
      ) %>
      <% item.sub_item(
        url: "#",
        label: "Inventory"
      ) %>
      <% item.sub_item(
        url: "#",
        label: "Disabled",
        disabled: true
      ) %>
    <% end %>
  <% end %>
<% end %>
