source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in polaris_view_components.gemspec.
gemspec

gem 'minitest'
gem 'pry'
gem 'foreman'

group :development do
  gem 'capybara'
  gem 'sqlite3'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
